server:
 port: 5200

 
spring:
 application:
    name: CustomerMS
 cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefix: config
        defaultContext: application
        profileSeparator: '::'
        format: YAML