server:
 port: 5100

 
spring:
 application:
    name: AddressMS
 cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefix: config
        defaultContext: application
        profileSeparator: '::'
        format: YAML